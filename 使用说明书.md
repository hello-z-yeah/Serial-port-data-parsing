# 串口数据解析 - 使用说明书

---

## 一、简介

**串口数据解析（V3.0 全双工协议型分析器 + 会话快照冷更新）** 是一款协议数据解析工具：
既能被动监听串口数据、按 JSON 协议自动解析，也能**主动发送指令**（协议组包 + Raw HEX/ASCII，支持毫秒级周期循环）；
程序更新升级时会自动记录当前串口、波特率、正在循环的指令等运行状态，新程序启动后**无感恢复**，用户察觉不到断点。

### 主要功能

- **粘贴解析**：将 Hex 数据粘贴到输入框，一键解析出指令含义
- **串口实时监控 + 指令发送（全双工同屏）**：连接串口设备后既能收也能发，TX/RX 毫秒级同屏区分显示
- **V3.0 协议编码器**：根据 JSON 协议自动生成完整帧（帧头 + 版本 + 命令字 + 长度 + Payload + CRC），用户只填字段名/值
- **毫秒级周期循环发送**：协议帧 / Raw HEX / Raw ASCII 三选一，最小 10ms 间隔循环下发
- **会话快照冷更新**：升级前自动保存串口参数、波特率、协议选择、正在循环的指令，重启后自动恢复
- **导入 Word 协议文档**：将产品的 Word 格式协议文档自动转换为 JSON 配置
- **多产品支持**：通过下拉框切换不同产品的协议，解析对应数据
- **日志 + 原始数据保存**：结构化解析日志（带 RX/TX 前缀）+ 原始 bytes 落盘（TX/RX 分别前缀）
- **一键出安装包**：绿色 exe → Inno Setup 安装包 exe（带开始菜单/桌面图标/卸载信息）

### 支持的协议格式

- 帧头标识（如 `0xA5A5`）
- 命令字识别
- 校验和验证（支持 sum、xor、crc8、crc16_modbus 等）
- 属性块解析（typeid + attrid + value 循环结构）
- 请求/响应方向自动识别

---

## 二、快速开始（3 种方式任选其一）

> 仅支持 Windows 64 位系统。

### 方式一：安装版 exe（推荐给最终用户）

双击 `release\串口数据解析_安装包_<版本>_x64.exe` → 一路「下一步」：
- 默认安装到：`%LocalAppData%\Programs\串口数据解析`（不需要管理员权限、默认可写、导入协议/保存日志不失败）
- 开始菜单自动新增分组：串口数据解析 / 使用说明书 / README / 卸载
- 安装向导末尾可选勾选「创建桌面快捷方式」
- 卸载：设置 → 应用 → 安装的应用 → 卸载；或开始菜单里的「卸载 串口数据解析」
- **重要**：卸载**不会删除**你自己导入到 `product/` 的自定义协议 JSON，重装/升级不会丢协议。

首次启动后会自动在安装目录建立 `product/` 目录，并复制默认协议；直接选产品开始使用即可。

### 方式二：绿色版 exe（拷到哪都能跑，不写注册表）

1. 拿到或自己产出 `串口数据解析.exe`（项目根目录 / 或 `dist\串口数据解析.exe`）
2. 双击运行：首次会在 exe 同目录创建 `product/`，复制默认协议
3. 选择产品协议 → 开始使用

> **适用场景**：发给现场工程师临时用；U盘拷走；不想在目标机器上"安装"。
> **缺点**：桌面/开始菜单没有快捷方式；不会在控制面板的已安装应用里出现。

### 方式三：Python 环境运行（开发/调试/改代码用）

**前提**：官方 Python 3.11+（安装时必勾：`Add python.exe to PATH` + `tcl/tk and IDLE`）

```
双击 启动程序.bat
```
- 首次运行自动装依赖（pyserial / python-docx / pyinstaller 等）
- 如遇网络故障会尝试 `packages/` 目录下的离线 whl

或者手动：

```bash
pip install -r requirements.txt
python exe_entry.py
```

---

## 三、界面功能详解

主界面采用 `Notebook` 双标签：**串口实时**（接收 + 解析 + 显示）、**指令发送**（主动下发 + 周期循环）。

### 3.1 顶部工具栏（两个 Tab 共享）

| 按钮/控件 | 功能说明 |
|-----------|---------|
| **产品协议** 下拉框 | 选当前要解析/发送的协议。首次默认 `v3_serial`（串口接入标准协议 V3.0） |
| **刷新** | 重新扫描 `product/` 目录，刷新协议列表（放了新 JSON 就点一下） |
| **导入Word协议** | 选 .docx → 自动提协议 → 存 `product/产品名.json` |
| **查看协议** | 看当前产品的帧结构、命令列表、属性表 |
| **保存日志** | 切换结构化解析日志的写入状态（带 RX/TX 方向前缀） |
| **保存原始数据** | 切换原始 bytes 落盘（每条带时间戳 + `TX ` 前缀区分发送/接收） |
| **清空** | 清空当前显示区/发送区内容 |
| **检查更新** | 触发冷更新：后台校验版本元数据 → 下载新版 exe → 快照串口状态 → 替换并重启（见「第五章 会话快照冷更新」） |

### 3.2 粘贴解析模式

适用于已有 Hex 数据，需要手动解析的场景。

**操作步骤**：

1. 顶部下拉选产品协议
2. 在粘贴解析输入框里写 Hex（支持多行 / 多种分隔）：
   - 空格分隔：`A5 A5 03 24 00 00 71`
   - 逗号分隔：`A5,A5,03,24,00,00,71`
   - 连续：`A5A50324000071`
   - 0x 前缀：`0xA5 0xA5 0x03 0x24 0x00 0x00 0x71`
3. `#` 开头是注释，不解析
4. 方向：`auto` / `request`（主机→设备） / `response`（设备→主机）
5. **解析** 按钮，或快捷键 `Ctrl + Enter`

---

### 3.3 串口实时 Tab（接收 + 解析 + RX/TX 同屏）

#### 打开/关闭串口
1. 选协议（先选协议再打开串口，不然发送/解析拿不到配置）
2. 切到 **串口实时** Tab
3. **刷新** → 选 **串口**（COM3/COM5…）→ 选 **波特率**（默认 115200，常用：9600 / 19200 / 38400 / 57600 / 115200）
4. 可选：数据位 / 停止位 / 方向（方向仅影响默认收发方判断）
5. **开始监控**（点完立刻开始读串口 + 统计 RX/TX）
6. 关闭时点 **停止监控**（自动关闭周期发送、释放串口句柄）

#### 显示选项
| 选项 | 说明 |
|------|------|
| **HEX 显示**（默认勾选） | 切换显示区：勾= HEX 协议帧；不勾= ASCII 纯文本模式（发 ASCII 时用） |
| **详细模式** | 勾：展开每个字段逐行解析；不勾：一行摘要 |
| **自动滚动** | 勾：自动滚到最新；不勾：可手动翻历史 |
| **发送方单选（调试用）** | 在串口实时上方：主机/模组 二选一；仅用于 UI 记录当前"我是哪一端"，和真实收发无关 |

#### TX/RX 同屏区分（V3.0 重要能力）
- **显示区**：发出去的帧会以 **`[TX]` 红色粗体** 前缀插入到日志流中；接收帧是默认格式（带方向标签 request/response）
- **状态栏**（右下）：改成 `RX <n>  TX <m>  错误 <k>  缓冲 <x>B`
  - `RX`：已经解析到的 **接收** 帧数
  - `TX`：已经成功写到串口的 **发送** 次数（每次 send_raw / 协议 send 都会 +1）
  - `错误`：校验失败 / 解析错误 / 打开失败次数
  - `缓冲`：帧同步器里还没凑齐完整帧的残留 bytes
- **结构化日志**（点「保存日志」生成的文件）：每条前面加 `[RX]` / `[TX]`
- **原始数据落盘**（点「保存原始数据」生成的文件）：每条前面加时间戳；发送帧额外前缀 `TX `，方便后期脚本过滤。

---

### 3.4 指令发送 Tab（协议组包 + Raw + 毫秒级周期循环）

切到「指令发送」Tab。界面分 5 块：**发送模式** / **协议参数**（协议模式时显示） / **Raw 内容**（Raw 模式时显示） / **发送操作**。

#### 模式 1：协议模式（自动组帧 + CRC，推荐）
1. **发送模式** 选「协议模式（自动组帧+CRC）」
2. **命令字 CmdID**：如 `0x20` / `0x01`（十进制也行，如 `32`）
3. **方向**：
   - `模组发送` → 该帧按 `request` 方向编码（典型：工具侧发请求）
   - `MCU发送` → 按 `response` 方向编码（典型：工具侧模拟设备回复）
4. **字段 JSON**：写 payload 字段，支持两种常用写法：
   - **普通 K/V 对象**：`{"value": 1, "msg_id": 7}`
   - **属性列表 attrs 简写**：`{"attrs": [[attrid, True/False(是否含类型), value], [0x12, True, 0x01]]}`（V3.0 单播/查询属性这种结构常用）
   - 也可以直接写 **原始 data bytes**：传 `{"__raw_payload__": "11 22 33"}` 覆盖协议 encoder 自己拼 payload 的逻辑
5. **▶ 发送一次**：立刻编一帧写进串口 → 串口实时里会立刻跳出一条红色 `[TX]`

#### 模式 2：Raw HEX（发任意字节流，不做协议）
1. **发送模式** 选「Raw HEX」
2. Raw 内容输入：`AA BB CC 0D 0A`（空格/逗号/换行/0x 前缀都可，会自动清空白）
3. **▶ 发送一次** → 原样写入串口。校验、帧头、长度都不自动加。

#### 模式 3：Raw ASCII（发文本，如 AT 指令、调试字符串）
1. **发送模式** 选「Raw ASCII」
2. Raw 内容输入：普通文本（回车就是 `\r\n`）
3. **▶ 发送一次** → 按 UTF-8 编码写进串口。ASCII 模式下请记得把「串口实时」Tab 的 **HEX 显示** 取消勾选，不然显示出来不直观。

#### 周期循环发送（毫秒级）
三种发送模式都支持循环下发：
1. **间隔(ms)** Spinbox：最小 10ms；过小会被自动 cap 到 10ms
2. 可选勾「启用循环」（只是打标记，真正启动要按下按钮）
3. **▶ 开始循环** → 按钮立即变成 **⏹ 停止循环**，此时：
   - 立刻发一次
   - 每到间隔（ms）再发一次，直到按停止 / 停止监控 / 串口意外断开
4. **停止监控** 会自动取消循环（避免出现"按钮还显示在发，串口已经关了"的假象）

#### 其他按钮
| 按钮 | 作用 |
|------|------|
| **复制当前帧 HEX** | 根据当前模式把"即将发送出去的完整 bytes"转成 HEX 空格分隔格式，复制到剪贴板。协议模式：复制 = 编码器出的完整帧（含帧头/CRC） |
| **清空输入** | 清空协议 fields + Raw 文本 |

> **冷更新恢复能力**：本 Tab 全部内容（模式、CmdID、fields JSON、Raw 文本、是否在循环、间隔多少 ms）在「检查更新」触发升级时会写入会话快照；新实例启动后如果打开串口成功，会自动按原间隔重新挂起循环发送——这就是"无感断点续接"的 TX 侧。详见第五章。

---

### 3.5 日志与原始数据保存（双写入 + RX/TX 区分）

两个按钮都在顶部工具栏：
- **保存日志**（结构化解析日志，文本格式）：默认文件名 `serial_log_时间戳.log`
  - 每条前带 `[RX]` / `[TX]`；RX 帧按 OK/ERR + 校验 ✓/✗ 展示；TX 帧内容和显示区一致
  - 升级重启后如果文件还在，会自动以追加模式打开，并写入分隔行 `===== 更新重启后继续记录 YYYY-MM-DDTHH:MM:SS =====`
- **保存原始数据**（纯 bytes 落盘）：默认文件名 `serial_raw_时间戳.txt`
  - HEX 模式：每行 `[时间戳] <空或"TX ">AA BB CC ...`
  - ASCII 模式：每行 `[时间戳] <空或"TX ">一行文本`
  - 超过 50MB 自动分割成 `_1.txt`、`_2.txt` …

---

## 四、导入产品协议

### 4.1 从 Word 文档导入（.docx）

1. 顶部 **导入Word协议** → 选 .docx
2. 输入产品名（留空自动从文档识别）
3. 导入完成弹摘要：命令数量 / 属性数量 / JSON 保存路径
4. 自动选中该产品，可立刻粘贴解析 / 串口实时 / 指令发送

**Word 文档表格格式建议**（识别率最高）：
- 帧结构表：含「帧头 / 版本 / 命令字 / 数据长度 / 校验」等字段定义列
- 命令字表：表头至少有「命令字编号 / 名称」两列（可选：方向、请求格式、响应格式）
- 属性表：表头至少有「属性ID / 名称 / 类型」三列（可选：单位、读写权限、枚举值）
- 合并单元格会显著降低识别率，尽量拆成标准二维表

### 4.2 手动加协议（JSON）

直接写 JSON 放到 `product/` 下即可。基本结构：

```json
{
  "product": "产品名称",
  "description": "产品说明",
  "version": "1.0",
  "frame": {
    "header": "0xA5A5",
    "header_size": 2,
    "cmd_offset": 3,
    "length_offset": 4,
    "length_size": 2,
    "checksum": { "algorithm": "sum", "length": 1 }
  },
  "commands": [
    {
      "cmd_code": "0x24",
      "name": "查询属性",
      "request":  { "format": "attr_query",  "name": "查询请求" },
      "response": { "format": "attr_report", "name": "查询响应" }
    }
  ],
  "attributes": {
    "1": { "name": "高度", "typeid": 1, "access": "rw", "unit": "mm" }
  }
}
```

模板参考：`product/_template.json`

---

## 五、会话快照冷更新（无感断点续接）

**触发方式**：顶部「检查更新」按钮；也可以由程序后台自动检测（如部署了更新服务器时）。

**整条链路**（对应「V3.0 方案」中的无感断点架构）：

1. **异步校验 + 静默下载**：后台线程取版本元数据 → 流式下载新版单文件 `串口数据解析.exe` → 比对 SHA256，确保完整性。
2. **安全断开 + 会话快照**：
   - 先停串口（同时自动取消周期发送 → 避免循环还在跑）
   - 把以下内容序列化写到项目根/安装目录的 `_update_session.json`：
     - 串口号、波特率、数据位、停止位、方向
     - 当前产品协议名、协议来源文件、HEX/ASCII 切换、详细模式
     - 是否正在写日志 / 原始数据（路径也记住，重启后文件存在就"追加"接上）
     - **指令发送完整状态**：模式 / CmdID / fields JSON / Raw 文本 / 是否在循环 / 循环间隔 ms
3. **独立替换 + 无感恢复**：
   - 轻量批处理等旧程序退出 → 覆盖新 exe → 新程序启动
   - 启动自动读到 `_update_session.json`：
     1) 先恢复 UI 所有下拉/勾选/文本（产品协议、发送面板全部内容）
     2) 尝试重开同一个串口
     3) 只有串口打开成功，才恢复"之前在循环"的定时器 → 循环按原间隔继续发
     4) 最后弹一个提示窗：更新成功 + 已恢复项；如果串口被拔了，会在状态栏明确写「COMx 无法恢复（请确认串口线是否已重新接入）」，不会伪造"已恢复"假象。

> 用户体感：点「检查更新」→ 弹更新确认 → 闪一下黑窗（替换 + 重启）→ 程序回来界面和刚才一样、串口继续在跑、循环还在发。

---

## 六、命令行模式（高级用户）

**前提**：Python 环境 + 依赖已装。

```bash
# 协议列表
python -m protocol_parser protocols

# 协议详情
python -m protocol_parser show --product v3_serial

# 单条解析
python -m protocol_parser parse --product v3_serial --hex "A5 A5 03 24 00 00 71"
python -m protocol_parser parse --product v3_serial --hex "..." --json

# 批量解析
python -m protocol_parser batch --product v3_serial --file data.txt
python -m protocol_parser batch --product v3_serial --file data.txt --out result.json

# 串口实时
python -m protocol_parser ports
python -m protocol_parser serial --product v3_serial --port COM3
python -m protocol_parser serial --product v3_serial --port COM3 --baudrate 115200 --detail --log monitor.log

# 粘贴交互（HEX 回车就解析）
python -m protocol_parser paste --product v3_serial
```

---

## 七、项目文件说明

| 文件/目录 | 说明 |
|-----------|------|
| `exe_entry.py` | 主入口（打包 exe 用它；`python exe_entry.py` 也能跑） |
| `protocol_parser/` | 源代码：协议解析器/串口采集/GUI/更新器/会话快照/Word 导入 |
| `product/` | 协议 JSON 目录；安装版位于 `%LocalAppData%\Programs\串口数据解析\product` |
| `product/v3_serial.json` | 串口接入标准协议 V3.0（默认） |
| `product/_template.json` | 新建产品 JSON 的模板 |
| `启动程序.bat` | 源码模式启动（自动装依赖 + `python exe_entry.py`）；安装/绿色版不需要 |
| `打包exe.bat` | 生成绿色 exe：**先 py_compile 自检语法 → 清旧产物 → PyInstaller 单文件 → 自动复制到项目根**；输出：`串口数据解析.exe` |
| `installer/串口数据解析.iss` | Inno Setup 安装包脚本（定义默认安装路径 / 快捷方式 / 卸载策略 / 中文语言） |
| `installer/一键出安装包.bat` | 一键：`打包exe.bat` → `ISCC 编译 iss` → 输出 `release\串口数据解析_安装包_<版本>_x64.exe` |
| `release/` | 安装包 exe 输出目录（运行一键脚本后会出现） |
| `_update_session.json` | 会话快照文件（仅在「检查更新」升级过程中短暂存在；若被中断，下次启动仍会尝试恢复） |
| `requirements.txt` | Python 依赖列表 |
| `README.md` | 项目入口页（GitHub/代码仓首页看的那个） |

---

## 八、常见问题

### Q1：双击 exe 没有反应？
- 首次启动需 1~3 秒解压 PyInstaller 单文件，请等待
- 杀毒/Defender 拦截：加信任目录或临时关闭
- 右键 → 属性 → 常规 → 如果出现「安全：此文件来自其他计算机…」点「解锁」

### Q2：串口无法打开 / 打开后立刻报错？
- 设备管理器确认串口存在、驱动正确
- 串口没被串口助手/其他工具占用
- 波特率 / 数据位 / 停止位与设备端一致
- 如果你是安装版 + 串口是 USB 转串口驱动对管理员权限有要求，可以右键快捷方式「以管理员身份运行」再试一次

### Q3：解析结果显示「校验失败」？
- 尝试重采一次；或粘贴时检查头尾有没有缺字节
- 产品协议下拉是否选对（选错产品 → 校验算法/长度错）
- 串口实时 + ASCII 模式下不要把文本当 HEX 协议看

### Q4：指令发送 Tab 协议模式提示「字段 JSON 解析失败」？
- 注意这是 **JSON**，不是 Python dict：字符串用 `"`（双引号）；true/false/null 小写；数字不能加前导 0
- 属性列表简写正确示例：`{"attrs": [[18, true, 1]]}`（18 十进制 = 0x12；JSON 数组嵌套）
- 实在想自己拼 bytes：写 `{"__raw_payload__": "11 22 33"}`（此时字段 JSON 只认这一个 key）

### Q5：周期循环发着发着突然停了？
- 常见 3 个原因：
  1) 手动点了「停止监控」→ 代码会自动取消循环（设计如此）
  2) 串口被拔 / 驱动报错 → on_error 触发后 collector 进入停止状态 → 下一个循环 tick 会把按钮切回 "▶ 开始循环" 并停掉
  3) 你打开了另一个串口工具把 COM 口占用了（同 2)）
- 重新打开串口成功后，如需继续循环，手动再点一次「▶ 开始循环」即可（或者走「检查更新」恢复流程自动恢复）

### Q6：安装版和绿色版选哪个？

| 维度 | 安装版（推荐给交付客户/同事） | 绿色版（推荐临时拷贝/现场） |
|------|-----|-----|
| 开始菜单快捷方式 | ✅ 自动建 | ❌ 自己拖 |
| 桌面快捷方式 | ✅ 可选勾 | ❌ 自己拖 |
| 控制面板里"已安装应用"+ 一键卸载 | ✅ | ❌ 直接删文件 |
| 默认安装路径可写（导入协议/写日志） | ✅ `%LocalAppData%\Programs\…` | ✅ 你拷到哪就是哪（别放 C:\Program Files） |
| UAC 弹框 | ❌（用户级安装，lowest） | ❌ |
| 升级时保留用户自定义 product/ 协议 | ✅ 卸载不删 product | ✅ 不动 product |

### Q7：如何重新打包绿色 exe / 安装版 exe？

**绿色 exe**：双击 `打包exe.bat` 即可（新版脚本会先做语法自检 + 自动复制到根）。  
**安装版 exe**：
1. 先装 Inno Setup 6：<https://jrsoftware.org/isdl.php>（安装时**必勾：Inno Setup Preprocessor + Chinese Simplified language**）
2. 双击 `installer\一键出安装包.bat`
3. 产物：`release\串口数据解析_安装包_3.0.0_x64.exe`

### Q8：更新后，串口还开着 + 指令发送正在循环，会不会丢？
- 走「检查更新」触发升级：**不会丢**（会话快照记录一切，且只有串口重新打开成功才恢复循环）
- 如果是你手动结束进程 / 断电：快照文件可能没写完，不会自动恢复，得手动重新开串口再启动循环。

### Q9：安装版的 product 协议目录在哪？
```
%LocalAppData%\Programs\串口数据解析\product
```
（把 `%LocalAppData%` 粘到资源管理器地址栏就能跳，一般对应 `C:\Users\<你>\AppData\Local\Programs\串口数据解析`）

---

## 九、协议 JSON 配置参考

### 帧结构（frame）

| 字段 | 类型 | 说明 |
|------|------|------|
| `header` | 字符串 | 帧头标识，如 `"0xA5A5"` |
| `header_size` | 整数 | 帧头字节数 |
| `ver` | 字符串 | 版本号，如 `"0x03"` |
| `ver_offset` | 整数 | 版本字段偏移 |
| `ver_size` | 整数 | 版本字段字节数 |
| `cmd_offset` | 整数 | 命令字偏移 |
| `length_offset` | 整数 | 数据长度字段偏移 |
| `length_size` | 整数 | 数据长度字段字节数 |
| `length_byte_order` | 字符串 | `"big"` / `"little"` |

### 校验（checksum）

| 字段 | 类型 | 说明 |
|------|------|------|
| `algorithm` | 字符串 | `sum` / `xor` / `crc8` / `crc16_modbus` |
| `length` | 整数 | 校验值字节数 |
| `covers` | 字符串 | 校验覆盖范围 |

### 命令（commands）

| 字段 | 类型 | 说明 |
|------|------|------|
| `cmd_code` | 字符串 | `"0x24"` |
| `name` | 字符串 | 名称 |
| `description` | 字符串 | 描述（可选） |
| `direction` | 字符串 | `request` / `response`（单向命令） |
| `request` / `response` | 对象 | 双向命令的请求/响应格式 |
| `fields` | 数组 | 字段定义 |

### 属性（attributes）

| 字段 | 类型 | 说明 |
|------|------|------|
| `name` | 字符串 | 属性名 |
| `typeid` | 整数 | 类型ID |
| `access` | 字符串 | `r` / `w` / `rw` |
| `unit` | 字符串 | 单位 |
| `range` | 字符串 | 数值范围 |
| `enum` | 对象 | 枚举值映射 `{"0": "关闭", "1": "打开"}` |

---

## 十、更新日志

### V3.0（本次：全双工 + 指令发送 + 会话快照冷更新 + 安装包一键产出）
- 重构主界面为 Notebook 双 Tab：串口实时 / 指令发送
- 新增协议编码器：`encode_frame(CmdID, fields, direction)` 与 `parse_frame` 对称（帧头/长度/CRC 自动）
- SerialCollector 新增 `send()` / `send_raw()` + `_write_lock` 线程安全写入 + `on_tx_sent` 回调
- 新增 TX/RX 同屏区分：红色 `[TX]` tag + 状态栏 RX/TX 双计数 + 日志/原始数据落盘带方向前缀
- 指令发送面板：协议模式 / Raw HEX / Raw ASCII 三选一；毫秒级周期循环（最小 10ms）
- 会话快照扩展到发送运行时状态：升级重启后自动恢复串口 + 正在循环的指令
- 安装包流水线：`installer/串口数据解析.iss` + `installer/一键出安装包.bat`（Inno Setup 6）
- 安装目录策略：用户级 `%LocalAppData%\Programs\串口数据解析`（lowest 权限、默认可写、卸载保留 product 协议）
- 打包脚本加固：`打包exe.bat` 先 py_compile 全量自检 + 清旧产物 + SKIP_PAUSE 支持一键流水线
- 字段 JSON 支持 `attrs` 简写三元组 + `__raw_payload__` 原始 bytes 覆盖

### V2.x（基础能力）
- 串口接入标准协议 V3.0 内置
- Word 协议文档导入 → JSON
- GUI 串口实时监控 + 粘贴解析 + 批量文件解析
- 日志保存 + PyInstaller 绿色单文件 exe
