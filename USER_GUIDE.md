# SerialX 用户说明书 v3.1.8

> 原名 Super Max Serial Tool（SMST）
> Windows 串口协议分析与模拟 MCU 工具

---

## 目录

1. [概述](#1-概述)
2. [安装与启动](#2-安装与启动)
3. [界面总览](#3-界面总览)
4. [串口接收分析页](#4-串口接收分析页)
5. [模拟 MCU 工具页](#5-模拟-mcu-工具页)
6. [串口连接与监控](#6-串口连接与监控)
7. [数据发送](#7-数据发送)
8. [产品 JSON 管理](#8-产品-json-管理)
9. [属性实时监控与操作](#9-属性实时监控与操作)
10. [自动回复引擎](#10-自动回复引擎)
11. [协议日志与解析](#11-协议日志与解析)
12. [原始数据保存](#12-原始数据保存)
13. [协议命令参考](#13-协议命令参考)
14. [日志级别说明](#14-日志级别说明)
15. [字体与高 DPI](#15-字体与高-dpi)
16. [数据与日志位置](#16-数据与日志位置)
17. [快捷键](#17-快捷键)
18. [常见问题排查](#18-常见问题排查)
19. [版本历史](#19-版本历史)

---

## 1. 概述

**SerialX** 是一款专为嵌入式/物联网设备调试设计的 Windows 桌面应用，核心能力：

| 能力 | 说明 |
|---|---|
| **串口收发** | 支持 HEX / ASCII 双模式，任意波特率，数据位/停止位可配 |
| **协议解析** | 自动解析 V3 串口协议帧，校验和验证，字段级展示 |
| **产品建模** | 通过 JSON 定义产品属性、权限、枚举和范围，无需改代码 |
| **实时属性** | 属性值实时刷新，支持手动上报与批量上报 |
| **自动回复** | 模拟 MCU 角色，自动回复心跳、状态查询和命令下发 |
| **数据持久化** | 原始帧分卷保存，支持磁盘故障告警 |
| **日志分析** | 彩色分级日志，TX/RX 标签化，属性前缀高亮背景 |

**技术栈**：Python 3.11–3.14 + PySide6 + qfluentwidgets

---

## 2. 安装与启动

### 系统要求

- Windows 10 / 11 x64
- 可用 USB 串口或虚拟串口（如 CH340、CP2102、FTDI）
- 约 200 MB 磁盘空间

### 安装

1. 双击 `SerialXSetup3.1.8_x64.exe`
2. 按向导完成安装，默认安装目录：

```text
%LOCALAPPDATA%\Programs\SerialX
```

3. 安装完成后桌面和开始菜单会创建快捷方式

### 首次启动

启动后默认进入"串口接收分析"页面。首次使用建议：
1. 连接串口设备
2. 确认波特率等参数
3. 点击 **开始监控**

### 升级

软件不联网，无自动更新。升级流程：
1. 退出 SerialX
2. 运行新版安装包覆盖安装
3. 用户数据（配置、产品、日志）位于 `%LOCALAPPDATA%\SST_串口工具`，**覆盖安装不会删除**

---

## 3. 界面总览

```
┌─────────────────────────────────────────────────────────────┐
│  SerialX 3.1.8  [串口接收分析] [模拟MCU工具]    [⚙] [⊟] [✕] │ ← 标题栏
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  左侧：页面导航（接收分析 / 模拟 MCU）                       │
│  右侧：功能区                                                │
│                                                             │
│  ┌─ 接收分析页 ──────────────────────────────────────────┐  │
│  │ 串口配置  │ 接收工具栏 │ 发送面板                      │  │
│  │ 实时日志区（TX/RX 彩色标签 + 解析文本）                │  │
│  │ 原始 HEX 数据区                                       │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌─ 模拟 MCU 页 ────────────────────────────────────────┐  │
│  │ 产品选择 │ 实时属性表 │ 预置命令 │ 自动回复开关        │  │
│  │ 实时日志区（同上，但有属性前缀背景高亮）                │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌─ 状态栏 ────────────────────────────────────────────┐  │
│  │ 串口状态 │ 发送/接收计数 │ 数据保存状态 │ 丢弃计数    │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 两个页面的区别

| 特性 | 串口接收分析 | 模拟 MCU 工具 |
|---|---|---|
| 协议解析 | ✅ | ✅ |
| 产品 JSON | ❌ 不加载 | ✅ 必须加载 |
| 属性显示 | ❌ | ✅ 实时属性表 |
| 自动回复 | ❌ | ✅ 条件满足时启用 |
| 预置命令 | ❌ | ✅ |
| 用途 | 通用串口监听 | 模拟 MCU 设备行为 |

> ⚠️ **重要**：切换页面前必须先停止当前监控，再在目标页面重新开始监控。程序在开始监控时冻结模式，避免页面切换导致解析器混用。

---

## 4. 串口接收分析页

### 功能

- 通用串口数据监听，不依赖产品配置
- 实时显示收发帧（HEX 原始 + 解析文本）
- 支持手动 HEX/ASCII 发送
- 支持原始数据保存

### 界面布局

```
┌──────── 串口配置 ────────┐  ┌──── 接收工具栏 ────┐
│ 串口 波特率 数据位 停止位 │  │ [清空] [导出] [查找] │
│ [刷新] [更多] [开始监控] │  └────────────────────┘
└───────────────────────────┘
┌──── 实时日志（上） ──────────────────────────────┐
│ [08:00:01.000] [TX] A5 A5 03 01 ...              │
│  → 命令下发 (0x01) | MCU→模组 | 属性id:04 值:05  │
│    照明-模式YHQ                                  │
│ [08:00:02.000] [RX] A5 A5 03 24 ...              │
│  → 回复设备快照 (0x24) | MCU→模组 | 属性id:00... │
└──────────────────────────────────────────────────┘
┌──── 原始 HEX 数据（下） ────────────────────────┐
│ A5 A5 03 01 00 04 07 02 04 05 00               │
│ A5 A5 03 24 00 XX ...                           │
└──────────────────────────────────────────────────┘
┌──── 发送面板 ───────────────────────────────────┐
│ [HEX模式]  A5 A5 03 01 00 04 07 02 04 05 00    │
│ [ASCII模式]  [文本框]                            │
│ [发送] 循环: [1] 次 间隔: [1000] ms              │
└──────────────────────────────────────────────────┘
```

### 日志格式说明

每条日志包含以下部分，各部分有不同的背景色：

```
[时间戳] [TX/RX标签]   原始 HEX 数据
→ 命令名 (0xXX) | 方向 | 属性id:XX 值:XX  属性名称值
  ↑无背景          ↑背景              ↑背景前缀        ↑无背景
```

| 元素 | 颜色 | 背景 |
|---|---|---|
| 时间戳 | `#2E86FF` 蓝色 | 无 |
| `→` 箭头 | 解析色 | 无 |
| 命令名 + 方向 | 解析色 | 浅绿/浅蓝（TX/RX） |
| `属性id:XX 值:XX` | 解析色 | 浅绿/浅蓝 |
| 属性名称值 | 解析色 | 无 |

**TX**（发送）标签用浅绿底 `#E8F5E9`，**RX**（接收）标签用浅蓝底 `#E3F2FD`。

---

## 5. 模拟 MCU 工具页

### 功能

- 加载产品 JSON 配置
- 实时属性监控与操作
- 自动回复心跳/命令
- 预置命令快速调用

### 界面布局

```
┌─ 产品与控制 ──────────────────────────────────────────┐
│ 产品: [照明产品_v1.2  ▾]  [导入] [管理产品]           │
│ 自动回复: [开关]   [全局启动] [心跳周期: 5s]         │
└──────────────────────────────────────────────────────┘
┌─ 实时属性表 ──────────────┐  ┌─ 预置命令 ──────────┐
│ ID  │ 名称      │ 当前值 │ │ [+ 添加] [保存]      │
│ 00  │ 灯-开关   │ 关闭   │ │ 心跳     → 0x20     │
│ 01  │ 灯-色温   │ 6400K  │ │ 读快照   → 0x24     │
│ 04  │ 照明-模式 │ YHQ    │ │ 开灯     → 0x01     │
│     │           │ [上报] │ │ 关灯     → 0x01     │
│     │           │ [全选] │ │                     │
└───────────────────────────┘  └─────────────────────┘
┌─ 实时日志（同接收分析页格式，但支持属性前缀背景）────┐
│ [08:00:01.000] [RX] A5 A5 03 24 ...                │
│ → 回复设备快照 (0x24) | MCU→模组                   │
│  | 属性id:00 值:00 灯-开关关闭                     │
│  属性id:01 值:0640 灯-色温1600                     │
└──────────────────────────────────────────────────────┘
```

### 属性权限与操作

| 权限 | 模组可写 | MCU 可读 | 说明 |
|---|---|---|---|
| **只读** | ❌ | ✅ | 仅 MCU 上报，模组无法修改 |
| **读写** | ✅ | ✅ | 双方均可读写 |
| **只写** | ✅ | ❌ | 模组可写，MCU 不可上报 |

- 只读属性收到写命令时**不会修改**，也不回复成功
- 只写属性**不能**通过单属性或批量上报
- 批量"全选"只选择可上报的属性

### 0x24 快照日志格式

0x24 命令（设备快照）的解析日志采用**快照格式**：

```
→ 回复设备快照 (0x24) | MCU→模组                  ← 头部带背景
 属性id:00 值:00 灯-开关关闭                       ← 属性前缀带背景，名称值无背景
属性id:01 值:0640 灯-色温1600                       ← 同上
属性id:0B 值:helloworld 配置-默认灯光'helloworld'    ← 同上
```

每条属性的 `属性id:XX 值:XX` 前缀带浅色背景高亮，便于快速定位；属性名称值不带背景，保持清爽。

---

## 6. 串口连接与监控

### 操作步骤

1. 在 **串口** 下拉框选择目标设备（如 `COM3`）
2. 设置 **波特率**（默认 115200）
3. 展开 **更多** 设置数据位和停止位（默认 8 / 1）
4. 点击 **开始监控**

### 参数说明

| 参数 | 可选值 | 说明 |
|---|---|---|
| 波特率 | 9600 / 19200 / 38400 / 57600 / 115200 / 230400 | 设备通信速率 |
| 数据位 | 5 / 6 / 7 / 8 | 每帧数据位数 |
| 停止位 | 1 / 1.5 / 2 | 帧间停止位 |
| 流控 | None / RTS-CTS / XON-XOFF | 硬件/软件流控 |

### 停止监控

- 点击 **停止监控** → 按钮显示"正在停止…"
- 后台线程安全退出，完成后按钮恢复
- 若驱动未安全停止，程序会禁止再次启动当前窗口

---

## 7. 数据发送

### HEX 模式

- 输入格式：`A5 A5 03 01 00 04 07 02 04 05 00`
- 支持空格分隔，自动忽略多余空白
- 校验不合法时会在发送前提示

### ASCII 模式

- 直接输入 UTF-8 文本
- 按字节原样发送
- 显示为 ASCII 文本

### 循环发送

- 循环次数：正整数
- 间隔：毫秒（ms）
- 发送队列满时提示降低频率

### 发送队列机制

所有发送（手动、循环、自动回复）统一进入 **TX 队列**，由独立发送线程写入串口。这样确保：
- UI 不被串口阻塞
- 接收线程不受写入影响
- 自动回复帧逐帧标记，日志中准确显示 `(自动回复)`

---

## 8. 产品 JSON 管理

### 导入产品

1. 在模拟 MCU 页面点击 **导入** 按钮
2. 选择产品 JSON 文件
3. 程序自动校验属性 ID、类型、权限、枚举、范围等

### JSON 格式示例

```json
{
  "pid": 0x01,
  "model": "LightProduct",
  "version": "1.2",
  "attributes": {
    "0x41": {
      "name": "light-mode",
      "cn_name": "照明-模式",
      "typeid": 2,
      "access": "读写",
      "enum": {"5": "YHQ"},
      "snapshot_wire_id": 0x04,
      "initial_value": 0
    },
    "0x45": {
      "name": "light-switch",
      "cn_name": "灯-开关",
      "typeid": 0,
      "access": "读写",
      "initial_value": 0
    }
  }
}
```

### 类型 ID 对照表

| typeid | 类型 | 字节宽度 |
|---|---|---|
| 0 | bool | 1 |
| 1 | int8 | 1 |
| 2 | uint8 | 1 |
| 3 | int16 | 2 |
| 4 | uint16 | 2 |
| 5 | int32 | 4 |
| 6 | uint32 | 4 |
| 7 | int64 | 8 |
| 8 | uint64 | 8 |
| 9 | float | 4 |
| 10 | double | 8 |
| 11 | string | 可变 |

### 属性 ID 映射

产品定义中的属性 ID（如 `0x41`）是**内部逻辑 ID**，协议通信中使用 **线协议 ID**（`serialId` / `snapshot_wire_id`）。程序在内部自动完成映射转换。

---

## 9. 属性实时监控与操作

### 查看属性

加载产品并开始监控后，实时属性表自动显示：
- **ID**：线协议 ID（如 `04`）
- **名称**：产品中定义的中文名称
- **当前值**：最近一次收到的值
- **操作**：单属性上报

### 手动上报单个属性

1. 在实时属性表找到目标属性
2. 编辑"当前值"输入框
3. 点击右侧 **[上报]** 按钮

### 批量上报

- **全选**：仅选择可上报属性（排除"只写"权限）
- **批量上报**：将选中的所有属性值打包为一条 0x10 帧上报

### 属性值变更机制

- 收到上报/快照帧时，**只更新对应行**，不重建整张表
- 心跳和 ACK 帧不会造成输入框失焦
- 未知属性和非法业务值**不会伪装为执行成功**

---

## 10. 自动回复引擎

### 启用条件

自动回复仅在以下条件**同时满足**时启用：

```
当前监控页面 = 模拟 MCU 工具
      AND
  当前选择有效 JSON 产品
      AND
  自动回复总开关 = 开启
```

### 自动回复的命令

| 命令 | 触发条件 | 回复内容 |
|---|---|---|
| **0x20** 心跳 | 收到模组心跳帧 | ACK 心跳值 + 设备状态 |
| **0x21** 设备信息 | 收到查询设备信息 | PID / Model / MCU 版本 |
| **0x24** 设备快照 | 收到快照请求 | 所有属性当前值 |
| **0x01** 命令下发 | 收到属性写命令 | ACK + 属性状态同步 |

### 命令下发（0x01）事务处理

```
模组发送 0x01 帧 → 整帧校验 → 更新属性 → 发送 ACK → 发送状态同步
                        ↓                    ↓
              校验失败 → 不更新属性       回复成功/失败
```

- 整帧校验成功后才更新属性，确保原子性
- 回复帧标记为 `(自动回复)`，日志中清晰区分

### 心跳周期

默认 5 秒，可在自动回复区域调整。

---

## 11. 协议日志与解析

### 日志级别

| 级别 | 颜色 | 用途 |
|---|---|---|
| `[EMERG]` | 深红 | 致命错误 |
| `[ERROR]` | 红色 | 运行时错误 |
| `[WARN]` | 橙色 | 警告/异常 |
| `[NOTICE]` | 深黄 | 重要提示 |
| `[INFO]` | 蓝色 | 一般信息 |
| `[DEBUG]` | 灰色 | 调试信息 |
| `[TRACE]` | 浅灰 | 追踪 |

级别标签使用圆角背景样式，白字深色底，在彩色日志中易于识别。

### 协议帧格式

```
字节  内容
0-1   帧头（A5 A5）
2     帧类型
3     命令码
4-5   数据长度（大端）
6-N   数据域
N+1   校验和
```

### 方向标签

| 标签 | 含义 |
|---|---|
| `[TX]` | 发送帧（浅绿底） |
| `[RX]` | 接收帧（浅蓝底） |
| `模组→MCU` | 模组发往 MCU |
| `MCU→模组` | MCU 发往模组 |

---

## 12. 原始数据保存

### 开启保存

1. 在串口配置栏展开 **更多**
2. 勾选 **开始存储数据**
3. 设置保存路径和文件名前缀

### 文件格式

```text
serial_data_20260813_170000.dat        ← 首个文件
serial_data_20260813_170000_001.dat    ← 分卷（每 50 MB）
```

### 保障机制

| 场景 | 行为 |
|---|---|
| 队列写入速度不足 | 丢弃记录并在状态栏提示 |
| 文件创建失败 | 明确弹窗提示 |
| 磁盘空间不足 | 停止保存并提示 |
| 程序退出 | 排空队列后再关闭文件 |
| 磁盘故障后 | 不再假装保存成功 |

### 读取数据

每个 `.dat` 文件包含完整帧序列（帧头 + 命令 + 数据 + 校验），可在其他工具中解析回放。

---

## 13. 协议命令参考

### 命令码速查表

| 命令码 | 名称 | 方向 | 说明 |
|---|---|---|---|
| `0x01` | 命令下发 | 模组→MCU | 设置属性值，MCU 更新并回复 |
| `0x10` | 状态上报 | MCU→模组 | MCU 主动上报属性变化 |
| `0x20` | 心跳 | 双向 | 保活检测，回复心跳值 |
| `0x21` | 设备信息 | MCU→模组 | PID / Model / MCU 版本等 |
| `0x24` | 设备快照 | MCU→模组 | 所有属性当前值 |

### 命令详解

#### 0x01 命令下发

**请求格式**：
```
[0x01] [消息ID] [属性ID1 类型1 数据1] [属性ID2 类型2 数据2] ...
```

**响应格式**：
```
[0x01] [消息ID] [状态] [ACK属性列表]
```

#### 0x10 状态上报

**上报格式**：
```
[0x10] [属性ID1 类型1 数据1] [属性ID2 类型2 数据2] ...
```

#### 0x20 心跳

**请求**：`[0x20] [心跳值]`
**响应**：`[0x20] [心跳值] [设备状态]`

#### 0x24 设备快照

**请求**：`[0x24]`
**响应**：`[0x24] [属性ID1 类型1 数据1] [属性ID2 类型2 数据2] ...`

---

## 14. 日志级别说明

程序日志分为以下类型：

### 界面日志

在实时日志区显示的彩色日志，包含：
- 收发帧（TX/RX 标签 + 时间戳 + 原始 HEX）
- 解析文本（命令名、方向、属性语义）
- 系统提示（如"自动回复已启动"、"队列积压"等）

### 文件日志

- **位置**：`%LOCALAPPDATA%\SST_串口工具\logs\`
- **滚动**：按大小滚动，单文件最大 10 MB，保留 10 份
- **error.log**：仅记录真正的程序异常
- **普通提示**（超出范围、串口未连接等）不写入 error.log

---

## 15. 字体与高 DPI

### 自动适配

- 根据 Windows DPI 和显示器逻辑分辨率统一调整字体
- 实时属性、预置命令、表格、下拉列表使用同一字号
- 窗口移动到其他显示器时自动重新布局

### 手动调整实时日志字号

实时日志文本框保留独立字号：
- **快捷键**：`Ctrl` + 鼠标滚轮
- **范围**：8 – 24 pt
- **记忆**：各页面独立保存字号设置

### 响应式布局

| 窗口宽度 | 布局变化 |
|---|---|
| 宽屏 | 单行布局，按钮完整显示 |
| 中等 | 自动换行，分栏改为上下排列 |
| 窄屏 | 滚动条可用，Tooltip 查看完整文本 |

---

## 16. 数据与日志位置

### 用户数据目录

```text
%LOCALAPPDATA%\SST_串口工具\
├── config\           应用配置
├── products\         导入的产品 JSON
│   └── backups\      内置产品升级备份
├── data\             原始数据保存目录
└── logs\             日志文件
```

> 内置产品升级时会自动备份旧版本到 `backups/` 目录。

### 安装目录

```text
%LOCALAPPDATA%\Programs\SerialX\
├── SerialX.exe       主程序
├── resources\        图标、版本信息等
├── README.md
└── USER_GUIDE.md
```

---

## 17. 快捷键

| 快捷键 | 功能 |
|---|---|
| `Ctrl + 滚轮` | 调整实时日志字号（8–24 pt） |
| `Ctrl + C` | 复制选中的日志文本 |
| `Ctrl + F` | 在日志中查找 |
| `Ctrl + L` | 清空日志 |
| `Ctrl + S` | 保存原始数据 |
| `Alt + Tab` | 切换窗口 |

---

## 18. 常见问题排查

### Q1：串口下拉框为空

- 检查 USB 串口驱动是否安装
- 尝试点击 **刷新** 按钮
- 在设备管理器中确认 COM 端口号

### Q2：点击"开始监控"后无反应

- 确认未在其他窗口/程序中占用该串口
- 重启 SerialX 后重试
- 检查波特率是否与设备一致

### Q3：收到数据但解析为乱码

- 确认波特率、数据位、停止位与设备配置一致
- 检查线序（TX/RX 是否接反）
- 确认协议帧头是否为 `A5 A5`

### Q4：自动回复不生效

检查以下三个条件：
1. ✅ 当前页面为 **模拟 MCU 工具**
2. ✅ 已加载有效 JSON 产品
3. ✅ 自动回复总开关已开启

### Q5：属性值不更新

- 检查属性权限（只读属性收到写命令不修改）
- 确认线协议 ID（`snapshot_wire_id`）与实际一致
- 查看日志中是否有 `[WARN]` 提示

### Q6：磁盘写入失败

- 检查磁盘剩余空间
- 确认保存路径有写权限
- 查看日志中的 `error.log` 获取详细错误

### Q7：界面在高缩放下显示异常

- 使用 Windows 10/11 原生 DPI 支持
- 推荐缩放比例 100% – 150%
- 跨显示器拖动窗口时会自动重新布局

### Q8：如何重置所有设置

1. 关闭 SerialX
2. 备份 `%LOCALAPPDATA%\SST_串口工具\` 目录
3. 删除 `config\` 下的配置文件
4. 重新启动 SerialX

---

## 19. 版本历史

### v3.1.8

- 品牌重命名为 **SerialX**
- 解析日志文本增加分层背景高亮：
  - TX/RX 标签 → 浅绿/浅蓝背景
  - 箭头 `→` → 无背景
  - 命令名 + 方向 → 浅色背景
  - `属性id:XX 值:XX` 前缀 → 浅色背景
  - 属性名称值 → 无背景
- 0x24 快照日志采用快照格式（`属性id:XX 值:XX 名称值`）
- 产品 JSON 导入兼容更多名称字段
- UI 全量 Fluent 风格灰色 Tooltip
- 级别标签采用圆角背景样式

### v3.1.0

- 双实时数据窗口隐藏字号控件（Ctrl+滚轮调节）
- 实时属性名称按可用宽度换行
- 产品 JSON 导入兼容更多名称字段
- 发送面板 HEX/ASCII 独立维护

---

> **SerialX v3.1.8 用户说明书**
> 文档生成时间：2026-08-13
> 技术支持：请联系开发团队