# SST_串口工具 3.1.0 使用说明

## 1. 安装与启动

双击 `SST_串口工具_Setup_3.1.0_x64.exe`，按安装向导完成安装。默认目录为：

```text
%LOCALAPPDATA%\Programs\SST_串口工具
```

程序不会联网检查或下载版本。后续升级时退出旧版本，直接运行新版完整安装包覆盖安装即可。用户产品、配置、日志和串口数据保存在 `%LOCALAPPDATA%\SST_串口工具`，不会因为覆盖安装而被删除。

## 2. 页面说明

### 串口接收分析

用于普通串口接收、HEX/ASCII 显示、协议帧解析、日志观察和手动发送。该页面启动监控后不会加载或运行模拟 MCU 的隐藏解析器，也不会使用上一次 JSON 产品进行自动回复。

### 模拟 MCU 工具

用于导入产品 JSON、查看实时属性、手动属性上报、批量上报、预置命令和自动回复。自动回复仅在以下条件同时满足时启用：

```text
当前监控页面是模拟 MCU 工具
当前选择的是有效 JSON 产品
自动回复总开关已开启
```

切换页面前应先停止当前监控，再在目标页面重新开始监控。程序会在开始监控时冻结本次监控模式，避免页面切换造成解析器和属性中心混用。

## 3. 串口连接

1. 在“串口”下拉框中选择设备。
2. 选择或输入波特率。
3. 根据设备设置数据位和停止位。
4. 点击“开始监控”。

停止监控使用后台线程执行。按钮显示“正在停止…”时不要重复点击；线程完全退出后按钮才恢复。若驱动未能安全停止，程序会禁止再次启动当前窗口，防止重复串口句柄。

## 4. 发送与自动回复

所有手动发送、指令库发送和自动回复都会先进入 TX 队列，再由独立发送线程写入串口。这样可避免 USB 驱动写入或超时冻结界面和接收线程。

发送队列已满时会显示普通提示，用户应降低循环发送频率。自动回复帧通过 TX 元数据逐帧标记，两帧回复都会准确显示为自动回复。

## 5. 产品 JSON 与属性

导入或修改产品 JSON 时，程序会校验属性 ID、`serialId`、类型、权限、枚举、范围、步长和字符串长度。

属性权限规则：

| 权限 | 模组下发修改 | MCU 状态上报 |
|---|---:|---:|
| 只读 | 不允许 | 允许 |
| 读写 | 允许 | 允许 |
| 只写 | 允许 | 不允许 |

批量“全选”只选择可上报属性；只写属性不能通过单属性或批量状态上报。只读属性收到写命令时不会修改，也不会回复成功状态。未知属性和非法业务值不会伪装成执行成功。

属性值发生变化时，界面只更新对应行的“当前值”，不会删除并重建整个属性表。心跳和普通 ACK 不再造成输入框失焦。

## 6. 原始数据保存

开启原始数据保存后，数据写入后台队列并由独立写线程落盘。默认分卷大小为 50 MiB，分卷文件名示例：

```text
serial_data_20260805_204800.dat
serial_data_20260805_204800_001.dat
```

停止保存或退出程序时会先排空队列，再关闭并同步文件。以下情况会明确提示：

- 队列写入速度不足并丢弃记录；
- 文件无法创建；
- 目录无权限；
- 磁盘空间不足或写入失败；
- 写线程未能在限定时间内停止。

累计丢弃数量会显示在状态栏。发生磁盘故障后程序会停止保存，不会继续假装保存成功。

## 7. 数据与日志位置

```text
%LOCALAPPDATA%\SST_串口工具\config
%LOCALAPPDATA%\SST_串口工具\products
%LOCALAPPDATA%\SST_串口工具\data
%LOCALAPPDATA%\SST_串口工具\logs
```

在资源管理器地址栏输入 `%LOCALAPPDATA%\SST_串口工具` 可直接打开。

`error.log` 只记录真正的程序异常。超出范围、枚举错误、字符串过长、串口未连接、目录无权限等可修正问题使用普通提示，不再显示“程序遇到未知错误”。

## 8. 字体与高 DPI 显示

程序会根据 Windows DPI 和当前显示器的逻辑分辨率统一调整界面字体。模拟 MCU 工具中的实时属性、预置命令、表格表头、按钮、复选框、Pivot 页签和下拉列表使用同一自适应字号。将窗口移动到另一台显示器时会重新计算字号和行高。

实时数据文本框仍保留独立字号逻辑，但界面不再显示“字号”控件；可使用 Ctrl+鼠标滚轮在 8–24 pt 范围内调整。

## 9. 手工升级

1. 停止监控并退出程序。
2. 运行新的 `SST_串口工具_Setup_<版本>_x64.exe`。
3. 使用相同安装目录完成覆盖安装。
4. 启动新版并确认产品 JSON 和指令库仍在。

软件没有下载器、联网版本接口或单 EXE 自替换逻辑。必须使用完整安装包升级，避免“新 EXE + 旧依赖目录”混合。

## 10. 开发和测试

推荐双击项目根目录的 `SST_Build_Manager.pyw`。若 `.pyw` 没有正确关联到 Python，可双击 `SST_Build_Manager.vbs`。

构建管理器可以完成环境检查、依赖安装、自动测试、源码运行、文件夹版 EXE、便携版和 Windows 安装包构建。

命令行方式：

```cmd
python SST_Build_Manager.py test
python SST_Build_Manager.py build-installer
```

最终安装包：

```text
release\SST_串口工具_Setup_3.1.0_x64.exe
```

正式发布前应在没有 Python 的纯净 Windows 10/11 x64 虚拟机中验证安装、启动、串口拔插、连续接收、磁盘故障、覆盖升级和卸载。

## 18. 不同分辨率与 Windows 缩放下的布局

BuildFix19 采用 Qt 原生 Per-Monitor DPI v2，并根据控件实际字体重新计算按钮、标签、下拉框、SpinBox、页签和表格的最小尺寸。窗口从一台显示器移动到另一台显示器后会自动重新布局。

- 窄窗口中，顶部共享按钮会从标题栏移动到内容区，避免标题和按钮互相遮挡。
- 串口配置、接收工具栏、发送面板和模拟 MCU 操作栏会自动换行。
- 模拟 MCU 同时显示实时属性或预置命令且宽度不足时，主分栏自动改为上下排列。
- 产品导入和产品管理窗口使用可滚动内容区，按钮不会因屏幕工作区较小而跑出窗口。
- 表格保留横向滚动条，完整文本可通过单元格 Tooltip 查看。

实时数据框字号仍由各页面独立维护，不受全局 UI 布局适配影响；字号控件已隐藏，使用 Ctrl+鼠标滚轮调整。

## 19. BuildFix20 紧凑布局说明

串口配置区会根据内容区的逻辑宽度选择单行、两行或极窄布局。正常桌面宽度下，串口、刷新、波特率、更多和开始监控保持在同一行；操作按钮只按文字所需宽度显示。

展开“更多”后，数据位、停止位、文件名和保存路径显示在浅色分组面板中。文件名和路径输入框自动吸收剩余空间，“选择”“打开文件位置”和“开始存储数据”不会再被拉成整行。

模拟 MCU 的实时属性表为 ID 列保留固定安全宽度，发送列使用剩余空间。长名称可通过横向滚动或鼠标悬停 Tooltip 查看完整内容。
